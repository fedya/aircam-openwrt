--- a/src/response.c
+++ b/src/response.c
@@ -666,7 +666,7 @@ handler_t http_response_prepare(server *
 				if (slash) pathinfo = slash;
 			} while ((found == 0) && (slash != NULL) && ((size_t)(slash - srv->tmp_buf->ptr) > (con->physical.basedir->used - 2)));
 
-			if (found == 0) {
+			if (found == 0 || pathinfo) {
 				/* no it really doesn't exists */
 				con->http_status = 404;
 
